{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/**/*"
  ],
  "packageManager": "yarn@3.3.1",
  "scripts": {
    "app:serve": "yarn workspace @sss/app dev",
    "app:build": "yarn workspace @sss/app build",
    "lerna:init": "yarn lerna init",
    "lerna:version": "yarn lerna version"
  },
  "dependencies": {
    "lerna": "^8.1.8"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
